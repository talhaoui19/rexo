import { SigninCard } from "@/components";

const Page = () => {
  return (
    <section className="w-full mt-[7em] mb-20 select-none">
      <div className="w-full flex justify-center">
        <SigninCard />
      </div>
    </section>
  );
};

export default Page;
